import { IconBrandMastercard } from '@tabler/icons-react';
import { IconProps } from '../../Icon.types';

export function IconMastercard(props: IconProps) {
	return <IconBrandMastercard {...props} />;
}
