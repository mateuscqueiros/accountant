import { IconCaretUpDown } from '@tabler/icons-react';
import { IconProps } from '../Icon.types';

export function IconArrowDoubleVertical(props: IconProps) {
	return <IconCaretUpDown {...props} />;
}
