import { IconFilterFilled as TablerIconFilterFilled } from '@tabler/icons-react';
import { IconProps } from '../Icon.types';

export function IconFilterFilled(props: IconProps) {
	return <TablerIconFilterFilled {...props} />;
}
